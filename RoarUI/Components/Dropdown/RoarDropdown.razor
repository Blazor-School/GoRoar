@namespace RoarUI
@inject IJSRuntime JSRuntime

<RoarDependency ComponentName="dropdown" />

<wa-dropdown distance="@Distance" open="@Open" placement="@Placement" size="@Size" skidding="@Skidding" onwa-select="(e)=>console.log(e)" @ref="Element">
    @ChildContent
</wa-dropdown>

@code {
    [Parameter]
    public double Distance { get; set; }

    [Parameter]
    public bool Open { get; set; } = false;

    [Parameter]
    public DropdownPlacement Placement { get; set; }

    [Parameter]
    public DropdownSize Size { get; set; }

    [Parameter]
    public int Skidding { get; set; }

    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    [Parameter(CaptureUnmatchedValues = true)]
    public IReadOnlyDictionary<string, object>? AdditionalAttributes { get; set; }

    internal ElementReference Element { get; set; }

    // protected override async Task OnAfterRenderAsync(bool firstRender)
    // {
    //     if (firstRender)
    //     {
    //         var instance = DotNetObjectReference.Create(this);

    //         await JSRuntime.InvokeVoidAsync("subscribeEvent", Element, "wa-invalid", instance, nameof(TriggerInvalidAsync));
    //     }
    // }

    public void Test(EventArgs e)
    {
        Console.WriteLine("OK");
    }
}
